import React from 'react';
import styled from 'styled-components';

const StyledNoResults = styled.span`
  padding: 1.5rem;
`;

export const NoResults = () => <StyledNoResults>No results</StyledNoResults>;
